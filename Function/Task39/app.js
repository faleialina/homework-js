// 39. Напишите функцию, которая принимает массив строк и возвращает массив, где строки
// отсортированы в обратном алфавитном порядке.

const sotrStr = (str) => str.split('').sort().join('')

console.log(sotrStr('fgdsasdfbfsdfsdfc'));
